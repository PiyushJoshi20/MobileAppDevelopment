<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileAppDevelopment"
        tools:targetApi="31">

        <!-- Main Activity (Launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MobileAppDevelopment">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.MobileAppDevelopment" />

        <!-- Profile Activity -->
        <activity
            android:name=".ProfileActivity"
            android:exported="true"
            android:theme="@style/Theme.MobileAppDevelopment" />

        <!-- Forgot Activity -->
        <activity android:name=".ForgotPasswordActivity" android:exported="true" />

        <!-- All Pages Activity -->
        <activity android:name=".EmployeeInfoActivity" android:exported="true" />
        <activity android:name=".AttendanceActivity" android:exported="true" />
        <activity android:name=".PayLeaveActivity" android:exported="true" />
        <activity android:name=".LeaveSlipActivity" android:exported="true" />
        <activity android:name=".OvertimeActivity" android:exported="true" />
        <activity android:name=".PaySlipActivity" android:exported="true" />
        <activity android:name=".CircularActivity" android:exported="true" />
        <activity android:name=".LeaveApprovalActivity" android:exported="true" />
        <activity android:name=".OvertimeApprovalActivity" android:exported="true" />
        <activity android:name=".PayScaleApprovalActivity" android:exported="true" />

    </application>

</manifest>
